{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/antoglz/miRdeX-nf/blob/main/assets/schema_input.json",
  "title": "miRdeX-nf - params.input schema",
  "description": "Schema for the file provided with params.input",
  "type": "array",
  "items": {
      "type": "object",
      "properties": {
        "Id": {
            "type": "string",
            "errorMessage": "Identifier for the input sample, project, or group must exist."
        },
        "File": {
            "type": "string",
            "format": "file-path",
            "exists": true,
            "errorMessage": "The file must exist. Additionally, all entries in the File column must be either '.txt', '.fastq', '.fastq.gz' or '.tsv",
            "oneOf": [
                  { "pattern": "^\\S+\\.txt$" },
                  { "pattern": "^\\S+\\.f(ast)?q\\.gz$" },
                  { "pattern": "^\\S+\\.tsv$" }
          ]
        },
        "Metadata": {
          "type": "string",
          "format": "file-path",
          "exists": true,
          "errorMessage": "The file must exist. Additionally, all entries in the File column must be '.tsv'",
          "pattern": "^\\S+\\.(tsv|txt)$"
        },
        "Genome": {
          "type": "string",
          "format": "file-path",
          "exists": true,
          "errorMessage": "The file must exist. Additionally, all entries in the File column must be '.tsv'",
          "anyOf": [
            {
                "pattern": "^\\S+\\.(fna|fa|fasta)(\\.gz)?$" 
            },
            {
                "maxLength": 0
            }
          ]
        },
        "Group":{
          "type":"integer",
          "errorMessage": "The Group must be an integer"
        }
      },
    "required": ["Id", "File", "Metadata"]
  }
}